# Misc unsorted abbreviations

abbr --add back prevd
abbr --add which type -a

# Note, in zsh, I'm using -P here to resolve symlinks, but that isnt implemented yet in fish
abbr --add --set-cursor var cd $WRKSP/%
